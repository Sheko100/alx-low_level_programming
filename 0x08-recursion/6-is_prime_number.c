#include "main.h"

/**
 * is_prime_number - checks if prime number
 * @n: number to check
 *
 * Return: number if prime number
 * 0 if not
 */

int is_prime_number(int n)
{
	if (n < 1)
		return (0);

	return (1);
}
